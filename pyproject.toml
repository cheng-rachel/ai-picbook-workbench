[project]
name = "power-up-picture-book-forge"
version = "0.1.0"
description = "Local data foundation for Power Up Picture Book Forge"
requires-python = ">=3.10"

[tool.unittest]
start-directory = "06tests"

